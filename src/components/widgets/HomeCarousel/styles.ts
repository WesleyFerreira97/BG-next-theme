import { styled } from "stitches.config";

export const MainContentWrap = styled("div", {
    "@xsMax": {
        ".info-container": {
            maxWidth: "500px",
            // display: "none !important"
        }
    }
});


